[package]
name = "moonshot-cad-kernel"
version = "0.1.0"
edition = "2021"
description = "Sovereign 2D/3D CAD kernel — geometry primitives + git-like JSON-Lines document model"
license = "Apache-2.0"

# Standalone workspace: an independent moonshot crate, like moonshot-bim-engine.
[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
