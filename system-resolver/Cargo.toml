[package]
name = "system-resolver"
version = "0.1.0"
edition = "2021"
description = "Deterministic semantic command resolver, replacing legacy AI-dependent system-slm."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[workspace]
