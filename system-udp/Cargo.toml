[package]
name = "system-udp"
version = "1.0.0"
edition = "2021"
authors = ["PointSav Digital Systems AG"]
description = "Zero-Broker UDP Mesh routing for the PointSav Private Network."

[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
