[package]
name = "service-fs"
version = "1.0.1"
edition = "2021"
authors = ["PointSav Digital Systems"]
description = "Bare-Metal Unikernel File System Gatekeeper. Enforces WORM storage boundaries."
# benches/ references LedgerBackend/PosixTileLedger not yet implemented — disable autodetection
autobenches = false

[[bin]]
name = "service-fs"
path = "src/main.rs"
test = false

[[bin]]
name = "service-fs"
path = "src/main.rs"
test = false

[dependencies]
# Pure bare-metal enclave. Zero OS dependencies permitted.

[[bin]]
name = "service-fs"
path = "src/main.rs"
# no_std/no_main — cannot be tested with std test harness
test = false
