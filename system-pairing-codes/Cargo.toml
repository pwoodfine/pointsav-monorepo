[package]
name = "system-pairing-codes"
version = "0.1.0"
edition = "2021"
description = "Crockford base32 short-code generation, normalization, and Unicode QR rendering — shared between project-console pairing and PPN node-join ceremony."

[dependencies]
rand = "0.10"
qrcode = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
