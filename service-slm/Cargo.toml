[workspace]
resolver = "2"
members = [
    "crates/slm-core",
    "crates/slm-doorman",
    "crates/slm-doorman-server",
    "crates/adapter-hub",
    "crates/slm-mcp-server",
]
exclude = [
    "cognitive-forge",
]

[workspace.package]
edition = "2021"
license = "Apache-2.0 OR MIT"
rust-version = "1.85"

[workspace.dependencies]
slm-core = { path = "crates/slm-core" }
slm-doorman = { path = "crates/slm-doorman" }
adapter-hub = { path = "crates/adapter-hub" }

# Async runtime
tokio = { version = "1.40", features = ["full"] }

# HTTP
axum = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }

# Serialisation
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"

# Identifiers, time
uuid = { version = "1.10", features = ["v7", "serde"] }
chrono = { version = "0.4", default-features = false, features = ["clock", "serde"] }

# Errors
anyhow = "1"
thiserror = "2"

# Async trait support (BearerTokenProvider in slm-doorman)
async-trait = "0.1"

# Regex (used by slm-doorman::redact for sanitize-outbound parity with
# bin/capture-edit.py REDACTIONS; and by slm-doorman::apprenticeship for
# parsing the apprentice's YAML-frontmatter / fenced-diff response shape)
regex = "1"

# File locking for the apprenticeship promotion ledger (single-writer
# serialisation across processes per design-pass Q3)
fs2 = "0.4"

# Base64 for the SSH signature blob carried in `ApprenticeshipVerdict`
# wire shape (design-pass Q5)
base64 = "0.22"

# SQLite mailbox index — Option C dual-write store (MIT)
rusqlite = "0.31"

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"

[profile.dev]
opt-level = 0
debug = true
